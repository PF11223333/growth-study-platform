name: Publish reading edition
on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: Validated site bundle release
        required: true
        type: string
permissions:
  contents: write
  pages: write
  id-token: write
concurrency:
  group: growth-pages
  cancel-in-progress: false
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Download validated site
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          RELEASE_TAG: ${{ inputs.release_tag }}
        run: |
          gh release download "$RELEASE_TAG" --pattern 'site.zip.part*' --dir bundle
          cat bundle/site.zip.part* > bundle/site.zip
          unzip -q bundle/site.zip -d site
          test -f site/version.json
          test -f site/index.html
      - name: Reassemble and verify original materials
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: python scripts/assemble-materials.py
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
